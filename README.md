# ChatGPT data analysis tooling (early WIP)

Create a conda environment from environment.yml.

Request your ChatGPT data from the web UI. Download and extract it from the e-mail you receive. For now paste `conversations.json` into the `scripts` folder then `python conversations.py` with the environment active will give you a simple summary. 

